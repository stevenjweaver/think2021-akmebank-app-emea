package org.akme.accounts.api;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeAccountCommandResourceIT extends AccountCommandResourceTest {

    // Execute the same tests but in native mode.
}